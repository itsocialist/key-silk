# Dependencies
node_modules/

# Build output
dist/

# Vault data — NEVER commit secrets
# NOTE: anchored with a leading slash so this ONLY ignores the runtime vault
# data dir at the repo root — NOT the src/vault/ source directory.
/vault/
*.enc
*.enc.bak
audit.log

# Environment files
.env
.env.*
!.env.example

# Internal planning docs (not user-facing)
mcp-secret-server-spec.md
docs/two_day_offsite_transcript.md
MIGRATION_REPORT.md

# Agent config (internal dev tooling — kept local, not published)
/.agent/
/.agents/
/_agents/
/_agent/
.gemini/
.claude/
.rafter.json
CLAUDE.md

# Stray asset (duplicate of logo/social-preview)
docs/assets/key Silk.png

# OS artifacts
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
